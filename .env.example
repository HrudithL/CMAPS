# API port (must match Vite proxy). Use 8001 if 8000 is taken or blocked.
API_PORT=8000

# Production frontend build (GitHub Pages / static host)
# VITE_API_URL=https://cmaps-api.onrender.com
# VITE_BASE_PATH=/CMAPS/

# Production API (Render)
# CORS_ORIGINS=https://hrudithl.github.io
